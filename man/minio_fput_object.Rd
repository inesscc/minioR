% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minio_fput_object.R
\name{minio_fput_object}
\alias{minio_fput_object}
\title{Upload a Local File to MinIO}
\usage{
minio_fput_object(
  bucket,
  file,
  object = NULL,
  multipart = FALSE,
  use_https = TRUE,
  region = ""
)
}
\arguments{
\item{bucket}{Character. Name of the MinIO bucket.}

\item{file}{Character. Path to the local file to upload.}

\item{object}{Character or \code{NULL}. Object key (path) to use in the
bucket. If \code{NULL} or empty, defaults to \code{basename(file)}.}

\item{multipart}{Logical. Whether to enable multipart upload. Defaults
to \code{FALSE}.}

\item{use_https}{Logical. Whether to use HTTPS when connecting to MinIO.}

\item{region}{Character. Region string required by \code{aws.s3}.}
}
\value{
Logical. Returns \code{TRUE} if the upload was successful.
}
\description{
Uploads a local file to a MinIO bucket. If the object name is not provided,
the base name of the local file path is used as the object key.
}
\details{
This function wraps \code{\link[aws.s3]{put_object}} and adds input
validation and clearer error handling for MinIO environments.
}
\examples{
\dontrun{
minio_fput_object(
  bucket = "assets",
  file = "data/example.parquet",
  object = "raw/example.parquet"
)
}

}
