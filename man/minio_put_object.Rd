% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minio_put_object.R
\name{minio_put_object}
\alias{minio_put_object}
\title{Upload an Object to MinIO from Memory}
\usage{
minio_put_object(
  bucket,
  object,
  raw,
  content_type = NULL,
  multipart = FALSE,
  use_https = TRUE,
  region = ""
)
}
\arguments{
\item{bucket}{Character. Name of the MinIO bucket.}

\item{object}{Character. Object key (path) within the bucket.}

\item{raw}{Raw vector containing the object payload.}

\item{content_type}{Character or \code{NULL}. Optional MIME type (e.g.,
\code{"text/csv"}, \code{"application/octet-stream"}).}

\item{multipart}{Logical. Whether to use multipart upload. Defaults to
\code{FALSE}.}

\item{use_https}{Logical. Whether to use HTTPS when connecting to MinIO.}

\item{region}{Character. Region string required by \code{aws.s3}.}
}
\value{
Invisibly returns \code{TRUE} if the upload was successful.
}
\description{
Uploads an object to a MinIO bucket using an in-memory \code{raw} vector.
For maximum cross-platform compatibility, the payload is first written to a
temporary file and then uploaded using \code{\link[aws.s3]{put_object}}.
}
\examples{
\dontrun{
payload <- charToRaw("hello\n")
minio_put_object("vault", payload, "tests/hello.txt", content_type = "text/plain")
}

}
