% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minio_list_objects.R
\name{minio_list_objects}
\alias{minio_list_objects}
\title{List Objects in a MinIO Bucket}
\usage{
minio_list_objects(bucket, prefix = NULL, use_https = TRUE, region = "")
}
\arguments{
\item{bucket}{Character. Name of the MinIO bucket.}

\item{prefix}{Character or \code{NULL}. Optional prefix used to filter
object keys.}

\item{use_https}{Logical. Whether to use HTTPS when connecting to MinIO.}

\item{region}{Character. Region string required by \code{aws.s3}}
}
\value{
A character vector containing the object keys. If no objects
  are found, returns \code{character(0)}.
}
\description{
Lists the object keys stored in a MinIO bucket, optionally filtered
by a prefix (for example, simulating a folder structure).
}
\details{
This function is a thin wrapper around \code{aws.s3::get_bucket()},
providing a simplified and safer interface for MinIO environments.
}
\examples{
\dontrun{
minio_list_objects("my-bucket")
minio_list_objects("my-bucket", prefix = "data/raw/")
}

}
