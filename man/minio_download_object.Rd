% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minio_download_object.R
\name{minio_download_object}
\alias{minio_download_object}
\title{Download an Object from MinIO to Disk}
\usage{
minio_download_object(bucket, object, destfile, use_https = TRUE, region = "")
}
\arguments{
\item{bucket}{Character. Name of the MinIO bucket.}

\item{object}{Character. Object key (path) within the bucket.}

\item{destfile}{Character. Local file path where the downloaded object
will be saved.}

\item{use_https}{Logical. Whether to use HTTPS when connecting to MinIO.}

\item{region}{Character. Region string required by \code{aws.s3}.}
}
\value{
Invisibly returns \code{destfile}.
}
\description{
Downloads an object stored in a MinIO bucket and saves it to a local file.
Before downloading, the function checks whether the object exists using
\code{\link{minio_object_exists}}. If the object does not exist, an error
is raised.
}
\details{
The download is streamed directly to disk (i.e., the full object is not
loaded into memory).
}
\examples{
\dontrun{
minio_download_object(
  bucket = "assets",
  object = "path/file.parquet",
  destfile = "path/file.parquet"
)
}

}
