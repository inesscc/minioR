% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minio_get_csv.R
\name{minio_get_csv}
\alias{minio_get_csv}
\title{Read a CSV File from MinIO}
\usage{
minio_get_csv(bucket, object, ...)
}
\arguments{
\item{bucket}{Character. Name of the MinIO bucket.}

\item{object}{Character. Object key (path) of the CSV file within the bucket.}

\item{...}{Additional arguments passed to \code{read.csv()}, such as
\code{sep}, \code{header}, \code{stringsAsFactors}, \code{fileEncoding},
etc.}
}
\value{
A \code{data.frame} containing the contents of the CSV file.
}
\description{
Downloads a CSV object stored in a MinIO bucket and reads it into memory
as a \code{data.frame}. Internally, the function retrieves the object
contents as raw bytes using \code{\link{minio_get_object}} and then
delegates parsing to \code{\link[utils]{read.csv}}.
}
\details{
Additional arguments are passed directly to \code{read.csv()}, allowing
control over separators, headers, encoding, and other parsing options.
}
\examples{
\dontrun{
df <- minio_get_csv(
  bucket = "assets",
  object = "path/file.csv",
  sep = ";",
  header = TRUE,
  fileEncoding = "latin1"
)
}

}
