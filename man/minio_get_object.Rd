% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minio_get_object.R
\name{minio_get_object}
\alias{minio_get_object}
\title{Download an Object from MinIO as Raw Bytes}
\usage{
minio_get_object(bucket, object, use_https = TRUE, region = "")
}
\arguments{
\item{bucket}{Character. Name of the MinIO bucket.}

\item{object}{Character. Object key (path) within the bucket.}

\item{use_https}{Logical. Whether to use HTTPS when connecting to MinIO.}

\item{region}{Character. Region string required by \code{aws.s3}.}
}
\value{
A \code{raw} vector with the object contents.
}
\description{
Downloads an object from a MinIO bucket and returns its contents as a
\code{raw} vector. Before downloading, the function checks whether the
object exists using \code{\link{minio_object_exists}}. If the object does
not exist, an error is raised.
}
\details{
This function is intended as a low-level building block for higher-level
helpers (e.g., reading CSV/Parquet), centralizing validation and download
logic.
}
\examples{
\dontrun{
x <- minio_get_object(
  bucket = "assets",
  object = "path/file.parquet"
)
length(x)
}

}
