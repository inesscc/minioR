% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minio_fput_dir.R
\name{minio_fput_dir}
\alias{minio_fput_dir}
\title{Upload a Local Directory to MinIO (Preserve Structure)}
\usage{
minio_fput_dir(
  bucket,
  dir,
  prefix = NULL,
  recursive = TRUE,
  include = NULL,
  exclude = NULL,
  dry_run = FALSE,
  multipart = FALSE,
  use_https = TRUE,
  region = ""
)
}
\arguments{
\item{bucket}{Character. Name of the MinIO bucket.}

\item{dir}{Character. Local directory path to upload.}

\item{prefix}{Character or \code{NULL}. Optional object key prefix inside the
bucket. If provided, all objects will be uploaded under this prefix.
Example: \code{"raw/projectA"}.}

\item{recursive}{Logical. Whether to include subdirectories. Defaults to \code{TRUE}.}

\item{include}{Character vector of regex patterns. If provided, only files whose
relative path matches at least one pattern are included.}

\item{exclude}{Character vector of regex patterns. If provided, files whose
relative path matches any pattern are excluded.}

\item{dry_run}{Logical. If \code{TRUE}, do not upload anything; only return the
planned uploads. Defaults to \code{FALSE}.}

\item{multipart}{Logical. Whether to enable multipart upload. Defaults to \code{FALSE}.}

\item{use_https}{Logical. Whether to use HTTPS when connecting to MinIO.}

\item{region}{Character. Region string required by \code{aws.s3}.}
}
\value{
A \code{data.frame} with the upload plan and results. Columns:
  \code{local_file}, \code{object}, \code{uploaded}, \code{error}.
  In \code{dry_run = TRUE}, \code{uploaded} will be \code{FALSE} and \code{error} \code{NA}.
}
\description{
Uploads a local directory to a MinIO bucket preserving the relative folder
structure, similar to a "sync". Files are discovered under \code{dir} and
uploaded using \code{\link{minio_fput_object}}.
}
\details{
You can filter which files to upload using \code{include} and \code{exclude}
regex patterns applied to the relative path (POSIX style, forward slashes).
}
\examples{
\dontrun{
# Upload entire directory under a prefix
res <- minio_fput_dir(
  bucket = "assets",
  dir = "data/projectA",
  prefix = "raw/projectA",
  recursive = TRUE,
  exclude = c("\\\\\\\\.tmp$", "^\\\\.git/", "\\\\\\\\.DS_Store$"),
  dry_run = FALSE
)
}

}
