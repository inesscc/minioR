% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minio_remove_objects.R
\name{minio_remove_objects}
\alias{minio_remove_objects}
\title{Remove Multiple Objects from MinIO}
\usage{
minio_remove_objects(
  bucket,
  objects = NULL,
  prefix = NULL,
  pattern = NULL,
  dry_run = FALSE,
  quiet = TRUE,
  error_on_missing = TRUE,
  use_https = TRUE,
  region = ""
)
}
\arguments{
\item{bucket}{Character. Name of the MinIO bucket.}

\item{objects}{Character vector or \code{NULL}. Explicit object keys to remove.}

\item{prefix}{Character or \code{NULL}. Prefix used to list candidate objects
when \code{objects} is \code{NULL}.}

\item{pattern}{Character or \code{NULL}. Regex pattern applied to object keys
(after prefix listing). If \code{NULL}, all objects under \code{prefix} are selected.}

\item{dry_run}{Logical. If \code{TRUE}, does not delete anything; only returns
the deletion plan. Defaults to \code{FALSE}.}

\item{quiet}{Logical. If \code{TRUE}, suppresses progress messages. Defaults to \code{TRUE}.}

\item{error_on_missing}{Logical. If \code{TRUE} (default), missing objects cause
an error (consistent with \code{minio_remove_object}). If \code{FALSE}, missing
objects are recorded and skipped.}

\item{use_https}{Logical. Whether to use HTTPS when connecting to MinIO.}

\item{region}{Character. Region string required by \code{aws.s3}.}
}
\value{
A \code{data.frame} with columns: \code{object}, \code{removed}, \code{error}.
  In \code{dry_run = TRUE}, \code{removed} is always \code{FALSE} and \code{error} is \code{NA}.
}
\description{
Removes (deletes) multiple objects from a MinIO bucket. You can either:
\itemize{
  \item Provide an explicit vector of object keys via \code{objects}, or
  \item Select objects by listing \code{prefix} and filtering with a regex \code{pattern}.
}
}
\details{
Deletions are performed by calling \code{\link{minio_remove_object}} for each object.
}
\examples{
\dontrun{
# Remove explicit objects
res <- minio_remove_objects(
  bucket = "assets",
  objects = c("tmp/a.csv", "tmp/b.csv")
)

# Remove by prefix + regex pattern
res <- minio_remove_objects(
  bucket = "assets",
  prefix = "tmp/",
  pattern = "\\\\\\\\.csv$"
)

# Dry-run
plan <- minio_remove_objects(
  bucket = "assets",
  prefix = "tmp/",
  dry_run = TRUE
)
}

}
